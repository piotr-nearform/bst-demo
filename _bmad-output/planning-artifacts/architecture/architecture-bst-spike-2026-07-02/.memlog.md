---
scope: Double-Booking Guard feature spine (BST scheduling spike) — one feature, feature altitude
purpose: Build substrate for the dev-story agent + a reviewer explainer for the Tech Lead; the spike proves AI-authored-human-owned delivery, so the spine fixes only the calls that make the pipeline demonstrable and the P0 concurrency guarantee real
altitude: feature
updated: 2026-07-02T14:32
---

- (constraint) Inherited from constitution (binding, read-only): Azure-only + UK data residency; ATDD test-first (Playwright from AC); spec-as-contract; agents-draft-humans-approve. React/Node/Postgres/Playwright are INDICATIVE, deferrable until a story demands them.
- (version) Verified on web 2026-07-02: Azure Database for PostgreSQL Flexible Server supports btree_gist + EXCLUDE constraints (enabled via azure.extensions allow-list param + CREATE EXTENSION), on PG 16/17/18, available UK South. Makes addendum Option A viable under Azure/UK constraint.
- (direction) User chose Coaching path; deliverables = spine + a short reviewer explainer for Tech Lead (Simon) focused on why the concurrency guarantee holds.
- (decision) [ADOPTED] Concurrency mechanism = Option A, Postgres EXCLUDE USING gist over (person_id WITH =, daterange(start,end,'[]') WITH &&). Binds: FR3/FR4/FR5. Prevents: any read-then-check interleave window; two overlapping bookings for one person persisting. Rule: non-overlap is a DB constraint, NOT application logic; the losing INSERT fails with SQLSTATE 23P01.
- (decision) Consequence of Option A: datastore is now BOUND to PostgreSQL (Azure Database for PostgreSQL Flexible Server, UK South) — EXCLUDE USING gist is Postgres-specific. This hardens the previously-indicative DB choice to load-bearing; React/Node front/back remain indicative/deferrable. Surfaced to user, not silent.
- (decision) [ADOPTED] Paradigm = plain layered (UI -> API/service -> data). Binds: all. Prevents: speculative abstraction (hexagonal) on a throwaway app; a 'helpful' clash check leaking into the service/UI layer. Rule: the clash guard is enforced ONLY at the data layer (the EXCLUDE constraint); the service translates 23P01->409 and MUST NOT perform its own overlap check (no read-then-check anywhere).
- (decision) Loser contract (from FR5) = deterministic rejection at two layers. API returns HTTP 409 identifying the clash (person + dates); UI shows plain-language message keyed to a STABLE test hook (test id / ARIA role), not final copy. Binds: FR5. Prevents: the loser seeing a 500/timeout/silent no-op, or tests coupling to message wording.
- (decision) Date model = inclusive daterange with day granularity; FR3 inclusive-overlap ('any shared day, endpoints included') is expressed SOLELY by daterange(start,end,'[]') && in the constraint. Binds: FR1/FR3. Prevents: a second, drifting copy of the clash rule in app code. Rule: the DB constraint is the single source of the clash definition.
- (decision) Deployment target (user chose deployed-to-Azure-UK): Azure Database for PostgreSQL Flexible Server, UK South; btree_gist enabled via the azure.extensions server parameter AND CREATE EXTENSION in a version-controlled migration (non-obvious: without the allow-list param, CREATE EXTENSION is rejected). App on Azure PaaS (UK). Secrets (DB connection) via env/Key Vault, never in code (inherited).
- (constraint) Acceptance/P0 test execution must run against an ISOLATED database/schema (fresh per run), not a shared instance, so 'exactly one wins' stays deterministic and repeatable. The Azure demo env and the test env are the same platform but must not share mutable state.
- (assumption) [ASSUMPTION] Stack seed: React (front) + Node service remain INDICATIVE per constitution/addendum, confirmed at first-story scaffold; PostgreSQL + btree_gist + Playwright are load-bearing/confirmed. App host (Azure Container Apps vs App Service) deferred with a lean to Container Apps.
- (question) App host: Azure Container Apps vs App Service — deferred (does not cause cross-unit divergence for a single-app spike). Revisit when the first story scaffolds hosting.
- (event) Reviewer gate ran: lint clean; 4 parallel reviewers (rubric PASS-WITH-FIXES, web-verify PASS, reconcile, adversarial). Applying consolidated fixes.
- (decision) AD-6 (new): pin the 409 response body shape. Binds: FR5/AC1/AC3/AC4. Prevents: API and UI stories both obeying AD-3 yet not wiring together (API ships {personId,dates}, UI needs a human name) — AC4 green while AC1/AC3 break. Rule: 409 body is a fixed JSON contract: { code:'DOUBLE_BOOKING', personId, personName, start, end } (bare YYYY-MM-DD dates); UI renders personName.
- (decision) AD-2 extended: rollback contract. Prevents: enriching the 409 body via a SELECT in the already-aborted transaction (Postgres 25P02) -> 500, violating AD-3. Rule: on 23P01 the txn is rolled back and the 409 body is built from the request payload + caught error only; no follow-up query in the failed txn.
- (decision) AD-7 (new): the P0 race must be pinned deterministically. Binds: FR4/AC1. Prevents: an accidentally-serialized 'concurrent' test (single connection / commit-between) passing while only the sequential guard (AC3) was ever exercised — P0 green without proving the guarantee. Rule: each create is exactly one transaction; AC1 is proven by two genuinely concurrent committed transactions (service/DB-level), the UI two-context test complements but does not substitute for it.
- (decision) AD-4 extended: timezone-free dates. Prevents: coercing 'YYYY-MM-DD' through Date/timestamptz shifting a boundary by a day (UJ-1 is August, BST=UTC+1), flipping the exact 7-Aug-touches-7-10 case AC3 exists to nail. Rule: dates are bare YYYY-MM-DD in Thu Jul 2 02:29:58 PM BST 2026 columns; no timestamp/timestamptz type touches an assignment date anywhere in the pipeline.
- (constraint) Input & range validity (owned by API layer, not the DB): end<start and unknown person/job id must return 4xx (400/404), never an unmapped 500 (Postgres 22000/23503). start==end (single-day booking) is valid. FKs person_id/job_id NOT NULL. This is the other half of constitution rule 2 (validate/sanitise input).
- (constraint) DB connection via a single env var (DATABASE_URL-style), sourced from env locally/CI and Azure Key Vault in the deployed env; same platform (Postgres) across all, distinct isolated databases.
- (constraint) Delivery discipline carried as conventions (inherited from constitution rules 3-5 + PRD §7, not re-decided): ATDD red-first & never-weakened; one small reviewable PR (counter-pressure: AI large diffs); PR evidence set = AC->test table, spec-in-diff w/ author+timestamp, two-way AB# link; test hook = data-testid (not message copy).
- (direction) Did NOT add a 'thesis AD' for 'AI code safe because human owns it' (reconcile HIGH) — it is engagement thesis/process owned by the constitution, not a code-divergence invariant; honored via conventions instead to avoid duplicating process as architecture fact.
- (question) Migration tooling (raw SQL vs Prisma/Flyway/etc.) deferred — migrations are version-controlled SQL regardless; the constraint IS the spec. Revisit at first-story scaffold.
- (event) spine finalized
